{
  "name": "etf-flow-radar",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "npm run build && npx --yes serve dist",
    "build": "node scripts/build-site-v2.mjs",
    "test": "npm run build && node --test tests/rendered-html-v2.test.mjs"
  },
  "dependencies": {
    "html-to-image": "1.11.13"
  }
}
